<%= form_for(@link_group) do |f| %>
  <% if @link_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_group.errors.count, "error") %> prohibited this link_group from being saved:</h2>

      <ul>
      <% @link_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :intro %><br />
    <%= f.text_area :intro %>
  </div>
  <div class="field">
    <%= f.label :priority %><br />
    <%= f.number_field :priority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
