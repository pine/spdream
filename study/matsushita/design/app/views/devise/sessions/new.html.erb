<div class="panel-heading">ログイン</div>

<div class="panel-body">

<p>高知工科大学 附属情報図書館のウェブサイトを管理するには、ログインして下さい。
パスワードを忘れた方は<a href="#">こちら</a>から。</p>

<%= simple_form_for(
    	resource,
    	as:  resource_name,
    	url: session_path(resource_name),
    	html: { class: 'form-horizontal', role: 'form' }
    ) do |f|
%>
	<div class="form-group">
		<%= f.label(
		    	:email,
		    	label: '管理用 ID&nbsp;&nbsp;:',
		    	class: ' control-label col-sm-3'
		    )
		%>
		<div class="col-sm-8">
			<%= f.input_field :email, required: false, autofocus: true, class: 'form-control' %>
		</div>
	</div>
	
	<div class="form-group">
		<%= f.label(
		    	:email,
		    	label: 'パスワード&nbsp;&nbsp;:',
		    	class: ' control-label col-sm-3'
		    )
		%>
		
		<div class="col-sm-8">
			<%= f.input_field(
			    	:password,
			    	required: false,
			    	class: 'form-control'
			    )
			%>
		</div>
	</div>
	
	<!--    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %> -->
	
	<div class="form-group">
		<div class="col-sm-12 text-center">
			<%= f.button :submit, "ログイン", class: 'btn btn-default' %>
		</div>
	</div>
<% end %>

<!--
<%= render "devise/shared/links" %>
-->

</div>