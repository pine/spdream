# coding: utf-8
<div class="panel-heading">編集画面</div>

<div class="panel-body">
  <p class="well well-sm fadeout-msg"><%= notice %></p>
  
  <%= render 'form' %>
  
  <p><%= link_to '戻る', :action => 'faculty', :id => @thesis.faculty_id %></p>
</div>

<%= javascript_include_tag 'cms_theses' %>