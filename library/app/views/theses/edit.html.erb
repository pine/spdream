# coding: utf-8
<h1>編集画面</h1>

<div id="notice"><%= notice %></div>

<%= render 'form' %>

<%= link_to '戻る', :action => 'faculty', :id => @thesis.faculty_id %>

<%= javascript_include_tag 'cms_theses' %>