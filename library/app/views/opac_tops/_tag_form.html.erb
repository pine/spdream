<%= form_tag(
      opacplus_add_tag_path,
      method: :post,
      remote: true,
      class: 'form-horizontal',
      id: 'tag_add_form',
      role: 'form'
    ) do
%>
  <%= hidden_field_tag(:book_id, @books.id) %>
  
  <div class="form-group">
    <%= label_tag(
        :tag_id,
        "名前\u00a0:",
        class: 'control-label col-sm-3'
      ) %>
    
    <div class="col-sm-8">
      <%= select_tag(
          :tag_id,
          [],
          class: 'form-control', id: 'modal_tags'
        ) %>
    </div>
  </div>
  
  <div class="form-group">
    <div class="col-sm-12 text-center">
      <%= button_tag 'この内容でタグを追加する', class: 'btn btn-default' %>
    </div>
  </div>
<% end %>
